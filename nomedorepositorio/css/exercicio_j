#include <stdio.h>
#include <math.h>
int main()
{
   int A;
   printf("Digite o valor de A: \n");
   scanf("%d", &A);
   if(A%2==0)
   {
       printf("%d eh par", A);
   }
   else
   {
       printf("%d eh impar", A);
   }
}
